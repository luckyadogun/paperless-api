{
  "routes": [
    {
      "method": "GET",
      "path": "/invitees",
      "handler": "invitee.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invitees/count",
      "handler": "invitee.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invitees/:id",
      "handler": "invitee.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/invitees",
      "handler": "invitee.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/invitees/:id",
      "handler": "invitee.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/invitees/:id",
      "handler": "invitee.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
